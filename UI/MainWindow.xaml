<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="MainWindow"  SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Определяем 2 столбца -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Button Content="Проверка соиденения" Grid.Row="3" Grid.Column="1" x:Name="ButtonGetApi" Click="ButtonGetApi_Click" />
        <Button Content="Версия SQL" Grid.Row="5" Grid.Column="1" x:Name="ButtonGetSQLVersion" Click="ButtonGetSQLVersion_Click" />
        <Button Content="Копировать в буферобмена" Grid.Row="6" Grid.Column="7" Click="Button_Click" />
        <ToggleButton Content="Подключение" Grid.Row="1" Grid.Column="1" x:Name="ButtonConnect" Click="ButtonConnect_Click" />
        <TextBlock Text="" Grid.Row="1" Grid.Column="3" Grid.ColumnSpan="6" Grid.RowSpan="5" x:Name="TB"/>
        <!--Rectangle Fill="LightBlue" Grid.Row="1" Grid.ColumnSpan="2" -->
        
    </Grid>
</Window>
